---
import Layout from '../layouts/Layout.astro';
import ProjectCard from '../components/ProjectCard.astro';
import { projects } from '../data/projects.js';
---

<Layout title="مشاريعنا - شركة الايمان للتطوير العقاري">
  <div class="projects-page">
    <div class="page-header">
      <div class="container">
        <h1 class="page-title">مشاريعنا العقارية</h1>
        <p class="page-subtitle">
          استكشف مشاريعنا المتميزة، من الوحدات التي تم تسليمها إلى المشاريع الواعدة قيد الإنشاء.
        </p>
      </div>
    </div>

    <div class="page-content">
      <!-- Delivered Projects Section -->
      <section id="delivered" class="projects-section delivered-section">
        <div class="container">
          <h2 class="section-title">مشاريع تم تسليمها</h2>
          <div class="projects-grid">
            {projects.delivered.map(project => (
              <ProjectCard project={{...project, status: 'delivered'}} />
            ))}
          </div>
        </div>
      </section>

      <!-- Under Construction Projects Section -->
      <section id="under-construction" class="projects-section under-construction-section">
        <div class="container">
          <h2 class="section-title">مشاريع تحت الإنشاء</h2>
          <div class="projects-grid">
            {projects.underConstruction.map(project => (
              <ProjectCard project={{...project, status: 'under-construction'}} />
            ))}
          </div>
        </div>
      </section>
    </div>
  </div>
</Layout>

<style>
  .projects-page {
    padding-bottom: 4rem;
  }

  .page-header {
    background-color: transparent; /* Removed blue gradient */
    text-align: right; /* Align text to the right */
    padding: 160px 0 80px 0;
  }

  .page-title {
    font-size: 3.5rem;
    color: var(--primary); /* Changed from #fff */
    margin-bottom: 1rem;
  }

  .page-subtitle {
    font-size: 1.2rem;
    max-width: 800px;
    margin: 0; /* Remove auto margin */
    color: var(--text-dark); /* Changed from #fff */
    opacity: 0.95;
  }

  .page-content {
    padding: 0;
  }

  .projects-section {
    padding: 4rem 0;
  }

  .delivered-section {
      background-color: var(--background);
  }
  .under-construction-section {
      background-color: #fff; /* Make this section slightly different */
  }

  .section-title {
    text-align: center;
    font-size: 2.8rem;
    margin-bottom: 3rem;
    color: var(--primary);
  }
  
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 2.5rem;
  }

  @media (max-width: 768px) {
    .page-header {
      padding: 120px 0 60px 0;
    }
    .page-title {
      font-size: 2.8rem;
    }
    .section-title {
        font-size: 2.2rem;
    }
    .projects-grid {
        grid-template-columns: 1fr;
    }
  }
</style>